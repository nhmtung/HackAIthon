# Core PyTorch and GPU Compute
torch>=2.1.2

# High-Performance Serving and Inference (vLLM is not natively supported on Windows)
# Use Hugging Face Transformers pipeline for local Windows debugging, vLLM for Docker production deployment.
vllm>=0.3.3; sys_platform != 'win32'

# HuggingFace Ecosystem
transformers>=4.38.0
huggingface_hub>=0.20.0

# Orchestration and Tooling
langchain>=0.1.0

# Data Processing and Evaluation
pandas>=2.0.0
scikit-learn>=1.3.0
